# CI-only dependencies.
# Installed by .github/workflows/ci.yml before `pip install -e .` so that
# system-lib-bound packages (rasterio, geopandas, GDAL) are skipped when the
# CI runner doesn't have GDAL available.
# This is the minimum set needed to run `pytest tests/` from a clean checkout.
numpy>=1.24
pandas>=2.0
scipy>=1.11
scikit-learn>=1.3
matplotlib>=3.7
seaborn>=0.13
shapely>=2.0
requests>=2.31
python-dotenv>=1.0
pydantic>=2.0
fastapi>=0.100
uvicorn>=0.23
httpx>=0.24
streamlit>=1.28
plotly>=5.17
folium>=0.14
streamlit-folium>=0.13
pylatexenc>=2.10
pytest>=7.4
pytest-cov>=4.1
pytest-mock>=3.11
pytest-benchmark>=4.0
pytest-xdist>=3.3
hypothesis>=6.82
# Optional packages — needed for some tests but not gated by CI
# rasterio>=1.3  # requires GDAL system libs
# geopandas>=0.14  # requires GDAL
# torch>=2.0  # ~2GB; not needed for unit tests
# transformers>=4.30  # large
# huggingface_hub>=0.16
# mlflow>=2.7
# dvc>=3.0
