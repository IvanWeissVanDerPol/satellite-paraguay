# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
.installed.cfg
*.egg
build/
dist/

# Virtual environments
venv/
env/
ENV/

# IDE
.vscode/
.idea/
*.swp
*.swo
.DS_Store

# Testing
.pytest_cache/
.coverage
htmlcov/
.tox/

# Mypy
.mypy_cache/

# Data (large)
data/raw/*.tif
data/raw/*.tiff
data/raw/*/*.tif
data/processed/*.tif
data/cache/embeddings/*.npy
data/cache/timeseries/*.npz

# Logs
logs/
*.log

# OS
Thumbs.db
.DS_Store

# DVC
.dvc/cache
.dvc/tmp

# Weights
models/checkpoints/*.pt
models/checkpoints/*.pth
models/fine_tuned/*.pt
models/fine_tuned/*.pth

# Outputs (regenerable)
outputs/predictions/*.npy
outputs/figures/*.png
outputs/figures/*.pdf

# Coverage (CI artifact; regenerated each run)
coverage.xml
htmlcov/

# Jupyter
.ipynb_checkpoints/
*.ipynb_checkpoints

# Secrets
.env
*.env
secrets.yaml

# Papers (drafts)
papers/drafts/*.pdf
papers/figures/*.pdf

# Large rasters (downloaded, gitignored)
data/hansen/
data/sentinel2/
data/mapbiomas/
data/boundaries/*.geojson
outputs/p0011/figures/deforestation_timeline.gif
mlruns/

# Sensitive partner data — Scenario 2 mitigation
# (Real wildlife labels via Guyra partnership; INBIO yield data;
#  secrets directory for partner keys). These MUST NEVER reach the
#  repo. See docs/security/audit-round-2.md.
data/raw/inbio/
data/labels/
secrets/

# Local venv (created 2026-08-11 for tests)
.venv/

# Backup files (created 2026-09-03 for citation verification)
*.bak
*pre-verify*

# pytest-benchmark output
.benchmarks/
